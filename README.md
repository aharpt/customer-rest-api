# Customer REST API

## Overview
This is a REST API project that uses Spring Boot to allow for basic CRUD operations for a Customer/Product/Orders system. These APIS are secured via Spring Security and JWT. The Customer/Product/Orders data is stored in a PostgresSQL database that runs within docker.

## Technologies Used:
- Spring Boot
- Spring Security
- Maven
- PostgresSQL
- Docker